# 商家管理后台设计规范

本文档只约束商家管理后台的前端设计风格、布局方式、组件组织、交互规则和代码组织方式。它不规定后台必须包含哪些具体页面，也不维护接口清单；具体功能范围和接口字段以当前后端实现与后续业务需求为准。

## 一、定位

商家管理后台面向商家、主播或运营人员使用，应优先保证信息清晰、操作高效、状态明确。界面可以体现直播竞拍业务的实时感和商业感，但整体应保持后台产品的克制和可维护性。

设计目标：

- 深色、专业、适合长时间使用。
- 表格、表单、详情和状态反馈清晰。
- 商品、直播、竞拍等业务模块可以独立扩展。
- 页面不做营销落地页，不使用过度装饰。
- 不把尚未接入的数据伪装成真实生产能力。

## 二、视觉风格

### 1. 色彩

主色：

```css
#FF6A00
```

用于主按钮、当前菜单、关键状态、重要价格和主要操作。

辅助色：

```css
#FFD700
```

用于成交提示、高价提示、重要标签和需要强调的金额。

背景色：

```css
#0F1115
#151821
#1E222D
```

文字色：

```css
#FFFFFF
#D1D5DB
#9CA3AF
```

状态色：

```css
#22C55E /* 成功、启用、在线 */
#EF4444 /* 危险、错误、取消 */
#6B7280 /* 停用、下架、结束、弱化 */
```

不要使用紫色、蓝紫色或 Ant Design 默认浅色后台风格作为主视觉。

### 2. 字体

中文后台优先使用：

```css
font-family:
  "Alibaba PuHuiTi",
  "HarmonyOS Sans SC",
  "Microsoft YaHei",
  "PingFang SC",
  sans-serif;
```

价格、金额、倒计时等数字信息可以使用：

```css
font-family:
  "DIN Alternate",
  "Roboto Condensed",
  "Arial",
  sans-serif;
```

不要依赖默认 Inter 字体。

### 3. 圆角与边框

- 常规按钮、输入框、标签、弹窗圆角建议 `6px` 到 `8px`。
- 后台卡片圆角不超过 `8px`，避免过于柔软的营销感。
- 分割线使用低对比边框，例如 `#2A2F3A`。
- 表格、筛选区和详情区以清晰边界为主，不堆叠多层阴影。

### 4. 信息密度

后台界面应便于扫描和批量操作：

- 表格行高适中，不做过大的展示卡片。
- 标题字号克制，面板内标题不要使用英雄区大字号。
- 重要价格、状态、倒计时可以放大，但不要影响表格和表单的阅读。

## 三、整体布局

### 1. 页面框架

采用经典后台布局：

```text
Header
Sidebar | Content
```

Header：

- 高度建议 `64px`。
- 用于展示系统名称、当前商家信息、全局操作。
- 背景色建议 `#151821`。
- 底部使用 `1px solid #2A2F3A` 分割。

Sidebar：

- 宽度建议 `220px`。
- 菜单项根据实际业务模块配置。
- 当前选中项使用主色 `#FF6A00`。
- 不使用默认蓝色或紫色选中态。

Content：

- 主内容区以表格、表单、详情、操作栏为核心。
- 宽屏下保留足够横向空间，避免内容挤压。
- 小屏下允许表格横向滚动，侧边栏可折叠。

### 2. 页面结构

常规列表页建议包含：

- 页面标题区。
- 筛选搜索区。
- 主操作按钮。
- 数据表格。
- 分页。
- 空状态和错误状态。

常规详情页建议包含：

- 基础信息区。
- 状态信息区。
- 关键操作区。
- 关联记录区。

常规表单页建议包含：

- 必填字段提示。
- 字段级校验。
- 保存、取消、重置等操作。
- 提交中状态和提交结果反馈。

## 四、组件组织

### 1. 目录结构

推荐按“通用组件 + 业务组件 + 页面模块”组织：

```text
src/
  components/
    layout/
    common/
    business/
  features/
    auth/
    goods/
    live/
    auction/
    shop/
  services/
  stores/
  types/
  utils/
  styles/
```

如果当前项目还没有路由、状态管理或请求库，可以先保留轻量结构，不必为了规范强行安装依赖。

### 2. 通用组件

通用组件不绑定具体业务语义，建议包括：

- `AppLayout`
- `PageHeader`
- `DataTable`
- `SearchBar`
- `StatusTag`
- `PriceText`
- `ConfirmAction`
- `EmptyState`
- `LoadingState`
- `ErrorState`

通用组件应通过 props 接收配置，不直接写死商品、直播、竞拍等业务字段。

### 3. 业务组件

业务组件按领域放在对应 feature 或 business 目录中，例如：

```text
components/
  business/
    goods/
    live/
    auction/
```

或：

```text
features/
  goods/
    components/
  live/
    components/
  auction/
    components/
```

业务组件可以包含领域语义，例如商品状态标签、竞拍价格面板、直播状态面板等。组件命名使用 PascalCase。

### 4. 卡片规则

允许使用卡片承载独立信息块，但不要卡片套卡片。

错误示例：

```text
Card
  Card
    Card
```

正确示例：

```text
Panel
  Header
  Content
  Footer
```

如果信息属于同一块内容，优先使用分组、分割线和网格布局，而不是继续嵌套卡片。

## 五、状态与标签

### 1. 状态标签

所有状态都应使用统一的 `StatusTag` 组件表达，不在各页面重复写样式。

建议状态视觉：

- 进行中、直播中、竞拍中：主色 `#FF6A00`。
- 成功、启用、在线：绿色 `#22C55E`。
- 成交、重要完成态：金色 `#FFD700`。
- 取消、错误、危险：红色 `#EF4444`。
- 下架、结束、停用、未知：灰色 `#6B7280`。

状态标签文案以业务实际枚举为准，规范不固定具体枚举值。

### 2. 价格展示

金额和价格统一使用 `PriceText` 组件。

要求：

- 明确单位。
- 列表中保持紧凑。
- 详情和关键操作区可以放大。
- 危险或异常价格变化需要有明确提示。

建议样式：

```css
.priceText {
  font-weight: 700;
  color: #FF6A00;
}
```

成交价或特别强调金额可以使用 `#FFD700`。

### 3. 时间与倒计时

时间、倒计时和刷新状态应清晰表达来源：

- 普通时间使用统一格式化工具。
- 倒计时只在业务需要时展示。
- 如果没有实时推送能力，页面应提供刷新或轮询反馈，不要暗示强实时。

## 六、交互规范

### 1. 操作反馈

所有用户操作都必须有反馈：

- 提交中：按钮 loading 或禁用。
- 成功：轻提示或局部刷新。
- 失败：错误提示，必要时展示重试入口。
- 删除、取消、结束等危险操作：确认弹窗。

### 2. 表单

表单要求：

- 必填字段清晰标识。
- 错误信息贴近字段。
- 同一页面不要混用多套表单状态管理方式。
- 保存失败不清空用户已填写内容。
- 编辑表单应能正确回填已有数据。

复杂表单可以使用 `react-hook-form + zod`；如果项目未安装相关依赖，也可以使用现有 React 状态和轻量校验实现。

### 3. 表格

表格要求：

- 操作列固定在右侧。
- 批量操作需要明显区分危险级别。
- 空列表展示 `EmptyState`。
- 请求失败展示 `ErrorState`。
- 数据加载中保持表格结构稳定，避免布局跳动。

### 4. 确认操作

危险操作必须二次确认，例如删除、取消、结束、下架等。

确认弹窗文案应说明后果，不要只写“确定吗”。确认按钮使用危险色或主色，取消按钮保持低强调。

## 七、响应式

后台主要面向 PC，同时兼容平板宽度。

断点建议：

```text
< 768px：简化布局
768px - 1024px：折叠侧边栏
> 1024px：完整后台布局
```

小屏要求：

- Sidebar 可折叠。
- 表格允许横向滚动。
- 操作按钮可以收纳到更多菜单。
- 表单字段改为单列排列。
- 关键状态和主操作不能被遮挡。

## 八、技术与代码规范

### 1. TypeScript

所有组件使用 TypeScript。

组件 props 显式声明类型：

```tsx
type PriceTextProps = {
  value: number
  strong?: boolean
}
```

### 2. 命名

组件使用 PascalCase：

```text
GoodsTable.tsx
LiveStatusTag.tsx
AuctionPricePanel.tsx
```

Hook 使用 camelCase：

```text
useGoodsList.ts
useAuctionDetail.ts
```

工具函数使用语义化命名：

```text
formatPrice.ts
formatTime.ts
```

### 3. 请求封装

接口请求应集中封装，不要在组件中散落裸 `fetch`。

推荐：

```text
services/
  request.ts
  goodsApi.ts
  liveApi.ts
  auctionApi.ts
```

具体接口路径和字段以当前后端实现为准，不写入本设计规范。

### 4. 样式

样式应集中维护主题变量：

```css
:root {
  --color-primary: #FF6A00;
  --color-accent: #FFD700;
  --color-bg-page: #0F1115;
  --color-bg-panel: #151821;
  --color-bg-card: #1E222D;
  --color-text-main: #FFFFFF;
  --color-text-secondary: #D1D5DB;
  --color-text-muted: #9CA3AF;
  --color-danger: #EF4444;
  --color-success: #22C55E;
}
```

不要在不同组件中重复硬编码颜色。

## 九、Codex 生成要求

当使用 Codex 生成商家管理后台代码时，应遵守：

1. 只把本文档当作设计、组件和代码组织规范。
2. 不根据本文档臆造具体业务页面和接口。
3. 具体页面、字段、接口、按钮以用户需求和当前后端代码为准。
4. 保持深色后台风格，主色使用 `#FF6A00`。
5. 禁止紫色主题和默认浅色后台风格。
6. 组件可复用，表格、表单、状态、确认操作要完整。
7. API 请求集中封装，组件内不散落裸请求。
8. 未安装的第三方库不要直接引入，除非用户明确要求安装。
9. 没有真实后端能力的数据，不要伪装成可用生产功能。
