# goods-service 开发进度

## 2026-05-25 19:56:43 +08:00

【新增说明：2026-05-25，本段记录本轮需求澄清和文档补充进度】

### 本次目标

- 阅读 `docs/services/goods-service/` 下全部 Markdown 文档，并结合当前 goods-service 实现与 shop-service 项目规范梳理需求不明确处。
- 在继续编码前，找出会影响 proto、数据库表、核心业务规则、跨服务调用或接口语义的问题。
- 对可以保守推进的非核心问题追加设计假设到服务文档。

### 已完成

- 已阅读 `需求.md`、`商品服务设计.md`。
- 已对照当前 `api/proto/goods/v1/goods.proto`、`services/goods-service` 现有代码结构、配置、handler、model、repository、README 和 Makefile。
- 已在 `需求.md` 追加“需求补充”和“待确认问题”。
- 已在 `商品服务设计.md` 追加“设计补充”和“待确认问题”。
- 已确认当前代码大体遵循 gRPC-only、GORM repository、gRPC health check、配置环境变量前缀等 shop-service 风格。

### 未完成

- 尚未根据待确认问题调整 proto、数据库模型或业务代码。
- 尚未实现商品封面上传能力；当前倾向由 api-gateway 或独立 file-service 处理。
- 尚未加入 `category_id`；该字段是否进入本期模型仍待确认。
- 尚未接入 shop-service、live-service、auction-service、order-service 等跨服务校验。

### 阻塞点

- `category_id` 是否属于本期商品模型会直接影响数据库表、proto 和 handler/repository。
- 封面上传归属会影响 goods-service 是否需要 HTTP、文件存储或上传相关 proto。
- `shop_id` 鉴权注入与是否调用 shop-service 校验，会影响服务边界和跨服务依赖。
- 删除/下架是否需要校验直播、竞拍、订单引用，会影响跨服务调用或事件机制。

### 设计变更

- 本轮未删除原文，仅追加说明。
- 追加说明 goods-service 当前按内部 gRPC-only 服务处理，HTTP 路径和 multipart 上传暂视为接入层参考。
- 追加说明创建商品默认下架、默认逻辑删除过滤、分页默认值和最大页大小。
- 追加说明原建表 SQL 中 `status` 字段后缺少逗号，手写 SQL 需要修正。
- 追加说明当前批量查询返回已找到且未删除商品，暂不因部分缺失整体失败。

### 验证结果

- 本轮主要执行文档和代码阅读，尚未运行测试。
- 待需求确认后再根据变更范围执行 `gofmt`、聚焦测试和必要的 `go test`。

### 后续建议

- 优先确认 `category_id`、封面上传归属、`shop_id` 可信来源、商品可见性语义、删除/下架跨服务约束和批量查询缺失 ID 语义。
- 确认后再统一调整 proto、生成代码、model、repository、handler、README、Makefile 与测试，避免契约反复震荡。

## 2026-05-25 20:08:30 +08:00

【新增说明：2026-05-25，本段记录需求确认后的文档与 api-gateway 占位实现】

### 本次目标

- 根据用户确认结果补充 goods-service 文档。
- 在 api-gateway 中新增商品封面上传占位接口，当前只生成假 URL，不进行图片存储。

### 已完成

- 已确认并记录完全取消 `category_id`，不调整 goods-service proto、数据库表或核心代码。
- 已确认并记录 goods-service 默认保持内部基础数据语义：默认只过滤逻辑删除，不默认过滤上下架状态。
- 已确认并记录商品引用约束由 live/auction/order 等调用方处理，goods-service 本期不做跨服务阻断。
- 已确认并记录批量查询返回已找到且未删除商品，部分 ID 缺失不整体失败。
- 已在 api-gateway 新增 `POST /api/goods/cover/upload` 占位接口，校验 multipart `file` 后返回假 `cover_url`。
- 已在 api-gateway 上传占位逻辑中使用临时假 `shop_id=10001`，并保留 TODO 等待鉴权注入方案确定。

### 未完成

- 尚未实现真实图片存储、对象存储、file-service 或文件清理策略。
- 尚未实现 api-gateway 鉴权中间件和真实 `shop_id` 注入。
- 尚未为 goods-service 增加跨服务引用校验；该能力已确认本期不做。

### 阻塞点

- 真实上传能力依赖文件存储方案或 file-service 设计。
- 真实 `shop_id` 注入依赖 api-gateway 鉴权中间件方案。

### 设计变更

- `category_id` 已标记为废弃并保留原文供追溯。
- `/api/goods/cover/upload` 从 goods-service 直接实现调整为 api-gateway 占位实现。
- goods-service 查询、批量查询、删除/上下架跨服务约束的语义已按用户确认记录为设计补充。

### 验证结果

- 已新增 api-gateway 上传 handler 测试，等待执行 gofmt 和 go test。
- 已执行 `gofmt -w services/api-gateway/internal/handler/goods.go services/api-gateway/internal/handler/goods_test.go services/api-gateway/internal/router/http.go services/api-gateway/internal/bootstrap/app.go`。
- 已执行 `go test ./services/api-gateway/internal/handler ./services/goods-service/internal/...`，通过。
- 默认 Go build cache 位于用户目录时，`go test ./services/api-gateway/... ./services/goods-service/...` 曾因 `C:\Users\80468\AppData\Local\go-build\... Access is denied` 失败；改用临时 `GOCACHE` 后已执行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/...`，通过。
- 已执行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./...`，除既有 `pkg/logger` 在 Windows 上清理临时日志文件时 `app.log` 被占用导致失败外，其余 Go 包通过。

### 后续建议

- 下一步优先补 api-gateway 鉴权中间件，将 JWT 中的 shop_id 注入请求上下文。
- 文件存储方案确定后，将假 URL 逻辑替换为真实上传到对象存储或 file-service 的实现。

## 2026-05-25 20:19:17 +08:00

【新增说明：2026-05-25，本段记录 api-gateway 到 goods-service 的 gRPC 转发实现】

### 本次目标

- 模仿 api-gateway 转发 shop-service 的代码结构，实现网关到 goods-service 的 HTTP 到 gRPC 转发。

### 已完成

- 新增 api-gateway 的 goods-service gRPC client 连接与 client 构造。
- 新增 `goodsService.addr` 配置、默认地址 `127.0.0.1:9002` 和环境变量 `API_GATEWAY_GOODS_SERVICE_ADDR`。
- 在 api-gateway bootstrap 中初始化 goods-service gRPC 连接、创建 `GoodsHandler`，并在停止时关闭连接。
- 在 api-gateway 注册商品 HTTP 路由：创建、编辑、删除、详情、列表、当前商铺列表、批量查询、上架、下架，以及已有封面上传占位接口。
- 扩展 `GoodsHandler`，将 HTTP 请求转发到 `goodsv1.GoodsServiceClient` 的对应 gRPC 方法。
- `shop_id` 相关接口继续使用临时假数据 `10001`，并在代码中保留 TODO，等待鉴权中间件注入方案确定。
- 更新 api-gateway README，补充 goods-service 地址、商品接口列表和临时 `shop_id` 说明。
- 新增/扩展 api-gateway handler 测试，覆盖创建商品转发、当前商铺商品列表转发、批量查询转发和封面上传占位接口。

### 未完成

- 尚未实现真实鉴权中间件，`shop_id` 仍未从 JWT claims 或请求上下文注入。
- 尚未实现真实图片存储，封面上传仍只返回假 URL。
- 尚未为每一个商品 HTTP 路由补齐独立单元测试；本轮已覆盖代表性转发路径。

### 阻塞点

- 真实 `shop_id` 注入依赖 api-gateway 鉴权中间件方案。
- 真实上传能力依赖文件存储方案或 file-service 设计。

### 设计变更

- api-gateway 从仅承接商品封面上传占位接口，扩展为承接 goods-service 主要 HTTP API 并转发到内部 gRPC。
- goods-service 仍保持 gRPC-only，不直接启动 HTTP 服务。

### 验证结果

- 已执行 `gofmt`。
- 已执行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/...`，通过。
- 已执行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./...`，除既有 `pkg/logger` 在 Windows 上清理临时日志文件时 `app.log` 被占用导致失败外，其余 Go 包通过。

### 后续建议

- 下一步补 api-gateway 鉴权中间件，把登录签发的 JWT claims 中的 `shop_id` 注入到请求上下文，并替换 goods handler 中的假 `shop_id`。
- 后续可继续补齐各商品路由的错误映射和参数校验单测。

## 2026-06-05 13:24:32 +08:00

【新增说明：2026-06-05，本段记录商品接口鉴权与网关返回结构对齐结果】

### 本次目标

- 修复 goods-service 与 api-gateway 中商品接口的身份注入和外部 HTTP 请求/返回结构，使其按文档由网关承接 HTTP、由 metadata 透传当前商铺身份。

### 已完成

- 确认 goods-service gRPC handler 已从 `identity.ShopID(ctx)` 读取当前商铺身份，不再信任请求体中的 `shop_id`。
- api-gateway 商品创建、编辑、删除、当前商铺列表、上架、下架均继续从商家 JWT context 注入 `shop_id`。
- 将商品创建、编辑、详情的 HTTP `data` 调整为商品对象本身；批量查询 `data` 调整为商品数组；删除、上架、下架 `data` 调整为空字符串；商品时间格式调整为文档示例的 `YYYY-MM-DD HH:mm:ss`。
- 按文档注册 `POST /api/goods/cover/upload`，复用现有对象存储上传能力，返回 `data.cover_url`；该接口归入商家鉴权路由。

### 未完成

- 未调整 goods-service proto 字段，内部 gRPC 仍保留 `shop_id` 字段以兼容生成代码和内部调用结构，但服务端身份来源以 metadata/context 为准。

### 阻塞点

- 无。

### 设计变更

- 商品外部 HTTP 响应由 `{"goods": ...}`、`{"list": ...}` 收敛为文档定义的直接 `data` 结构。

### 验证结果

- 已运行 `gofmt`。
- 已运行 `$env:GOCACHE = Join-Path (Get-Location) '.tmp-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/... ./services/auction-service/...`，通过。

### 后续建议

- 如后续需要让 `/api/files/upload` 也强制鉴权，可单独按文件服务语义调整；当前仅商品封面上传按商品文档纳入商家鉴权。

## 2026-06-05 13:34:36 +08:00

【新增说明：2026-06-05，本段记录商品服务错误提示中文化结果】

### 本次目标

- 确认 goods-service gRPC 身份注入是否符合文档设计，并将商品服务中对外返回的英文错误提示替换为更准确的中文提示。

### 已完成

- 确认 goods-service gRPC server 已接入 `identity.UnaryServerInterceptor()`，受保护商品 RPC 从 `identity.ShopID(ctx)` 读取当前商铺身份。
- 将 goods-service 的参数错误、身份缺失、商品不存在和内部错误提示改为中文。
- 将 api-gateway 商品创建、编辑、批量查询转发入口的 `invalid request` 改为中文参数提示。

### 未完成

- 无。

### 阻塞点

- 无。

### 验证结果

- 已运行 `gofmt`。
- 已运行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/... ./services/auction-service/...`，通过。
