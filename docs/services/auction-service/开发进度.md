# 开发进度

## 2026-05-29 13:59:38 +08:00

【新增说明：2026-05-29，本段记录本轮开发前需求澄清进度】

- 本次目标：按 `livebid-microservice` 流程开发 `auction-service`，先阅读文档并完成需求澄清，再进入 proto 与服务编码。
- 已完成：阅读 `docs/services/auction-service/需求分析.md`、`docs/services/auction-service/服务设计.md`，并对照 `templates/go-service`、`services/shop-service`、`api/proto/shop/v1/shop.proto` 梳理实现边界。
- 未完成：尚未编写 proto、生成代码、复制模板或实现服务代码。
- 阻塞点：存在会影响 proto、数据库表、核心业务规则、跨服务调用和接口语义的待确认问题，需用户确认后继续编码。
- 设计变更：已在 `服务设计.md` 末尾追加“待确认问题”和“设计补充”，未删除或改写原设计内容。
- 验证结果：本阶段仅进行文档与代码上下文读取，未运行 gofmt 或测试。
- 后续建议：优先确认 gRPC-only 边界、`shop_id` 来源、状态枚举、手动开始/结束时间语义、Redis/RocketMQ 是否本期落地，以及是否需要 `room_id`。

## 2026-05-29 14:07:20 +08:00

【新增说明：2026-05-29，本段记录需求确认结果】

- 本次目标：记录用户对核心待确认问题的回复，作为后续 proto、数据库模型、跨服务调用和事件方案的实现依据。
- 已完成：确认服务保持 gRPC-only；HTTP 仅供 `api-gateway` 转发；创建竞拍需要调用 `goods-service`；状态不包含上架/下架；手动开始/结束使用服务器时间；字段沿用 `seal_price`；出价请求结构修正；本期采用 Redis/RocketMQ；不需要 `room_id`；删除仅逻辑删除竞拍主表。
- 未完成：尚未开始编码实现。
- 阻塞点：Redis/RocketMQ 依赖和本地生成工具可用性待编码验证。
- 设计变更：已在 `服务设计.md` 末尾追加“需求补充”，保留原文不删除。
- 验证结果：本阶段未运行 gofmt 或测试。
- 后续建议：先编写 `api/proto/auction/v1/auction.proto` 并生成代码，再复制模板实现服务。

## 2026-05-29 14:25:10 +08:00

【新增说明：2026-05-29，本段记录本轮 auction-service 首版开发结果】

- 本次目标：按确认后的需求完成 `auction-service` 首版 gRPC 微服务开发，覆盖 proto、生成代码、服务结构、模型、仓储、Redis/RocketMQ 方案、goods-service 校验、配置、README、Makefile 和验证。
- 已完成：新增 `api/proto/auction/v1/auction.proto`；生成 `gen/proto/auction/v1` 代码；从 `templates/go-service` 创建 `services/auction-service`；实现 gRPC-only 服务入口、bootstrap、router、config、model、MySQL repository、Redis 竞拍状态仓储、RocketMQ 事件发布、goods-service gRPC 校验客户端、auction gRPC handler；补充 `README.md`、`Makefile` 和配置加载测试。
- 已完成接口：创建竞拍、按 ID 查询、按商品查询、商铺分页列表、修改待开始竞拍配置、开始竞拍、用户出价、查询出价记录、手动结束、取消、逻辑删除。
- 未完成：尚未实现 RocketMQ 消费端监听并异步回写数据库；当前代码在发布事件后仍做了本地数据库快照更新，保证首版可查询和可验证。未实现 `api-gateway` HTTP 转发接入。
- 阻塞点：完整异步闭环依赖 RocketMQ 消费者部署策略、消费组命名、重试/死信策略和事件幂等表设计，当前文档尚未定义这些细节。
- 设计变更：按确认结论保持 gRPC-only；`shop_id` 当前请求缺省时使用固定值 `10001` 作为鉴权注入占位；创建竞拍调用 `goods-service` 校验商品归属；状态不包含上架/下架；手动开始/结束使用服务器时间；出价请求使用 `auction_id/user_id/bid_price/request_id`；不引入 `room_id`；删除仅逻辑删除竞拍主表。
- 验证结果：已运行 `protoc --go_out=. --go-grpc_out=. api/proto/auction/v1/auction.proto` 成功；已运行 `gofmt`；已运行 `go test ./services/auction-service/...` 通过；已运行 `go test ./...`，新增服务通过，但全仓因既有 `pkg/logger` 的 `TestNewWritesJSONToFile` 在 Windows 上清理临时日志文件时被占用而失败。
- 后续建议：补 RocketMQ consumer 与事件幂等表；补 Redis Lua 脚本单元测试或集成测试；补 api-gateway 到 auction-service 的转发；将固定 `shop_id=10001` 替换为鉴权注入值。

## 2026-05-29 14:33:27 +08:00

【新增说明：2026-05-29，本段记录 RocketMQ consumer 开发结果】

- 本次目标：自行定义并开发 RocketMQ consumer 监听、异步回写数据库、幂等表、重试/死信策略，并补充服务设计文档。
- 已完成：新增 `auction_event_consume_log` 模型和 GORM 自动迁移；新增事件消费仓储；新增 RocketMQ push consumer；实现 `auction_started/bid_accepted/auction_finished/auction_failed/auction_cancelled/auction_expire_check` 消费分发；实现出价记录幂等插入；实现基于 `version` 的竞拍状态防旧覆盖；实现延迟检查消息通过 Redis Lua 原子自动结束竞拍；接入 bootstrap 启动和停止流程；补充配置项 `consumerGroup/maxReconsumeTimes`。
- 未完成：尚未接入外部监控告警；尚未实现人工补偿后台或 CLI；尚未补 Redis/RocketMQ 集成测试。
- 阻塞点：本地未启动真实 RocketMQ/Redis/MySQL 环境，因此本轮验证以编译和单元测试为主。
- 设计变更：已在 `服务设计.md` 末尾追加 RocketMQ consumer、幂等表、重试和死信策略说明。
- 验证结果：已运行 `gofmt`；已运行 `go test ./services/auction-service/...` 通过。
- 后续建议：补充基于 testcontainers 或本地 Docker Compose 的 Redis/RocketMQ/MySQL 集成测试；为 `auction_event_consume_log.status=2` 增加告警和补偿入口。

## 2026-05-29 19:34:39 +08:00

【新增说明：2026-05-29，本段记录接口文档业务规则补充结果】

- 本次目标：根据当前 `auction-service` 代码实现，补充 `服务设计.md` 中接口文档的业务规则，重点覆盖创建竞拍活动。
- 已完成：阅读 `auction_grpc_handler.go`、竞拍/出价仓储、Redis 状态仓储、goods-service 客户端、RocketMQ 事件发布与消费代码；在接口文档中为创建、查询详情、按商品查询、商铺列表、修改配置、开始、出价记录列表、结束、取消、删除和用户出价补充业务规则。
- 未完成：未调整接口示例返回结构和旧 proto 示例字段编号，只在业务规则中标注当前代码实际行为。
- 阻塞点：无。
- 设计变更：未变更服务代码；仅按当前实现补充文档，明确 gRPC-only、shop_id 占位注入、参数校验、状态限制、Redis/RocketMQ 行为、MySQL 快照回写和逻辑删除规则。
- 验证结果：已查看文档 diff，确认业务规则段落已写入 `docs/services/auction-service/服务设计.md`。
- 后续建议：后续接入 `api-gateway` 时同步修正文档中的 HTTP 返回示例，使其与实际 RPC 返回对象和网关包装格式保持一致。

## 2026-05-29 19:37:47 +08:00

【新增说明：2026-05-29，本段记录接口文档结构调整结果】

- 本次目标：按用户要求，将每个接口的业务规则直接放到接口描述下方。
- 已完成：调整 `服务设计.md` 中全部接口的段落顺序，当前结构为“接口标题 -> 接口描述 -> 业务规则 -> 接口地址/RPC 方法 -> 参数与返回值”。
- 验证结果：已使用 `rg` 检查所有接口标题后的业务规则位置，确认创建、查询、列表、修改、开始、出价记录、结束、取消、删除、用户出价均已覆盖。

## 2026-05-29 19:41:26 +08:00

【新增说明：2026-05-29，本段记录业务规则口径调整结果】

- 本次目标：按用户反馈，将接口业务规则从实现细节说明调整为核心业务逻辑说明。
- 已完成：重写 `服务设计.md` 中各接口的业务规则，重点保留业务成功条件、状态流转、成交/流拍/取消/删除规则、出价成功后的核心效果和业务失败条件。
- 已移除：从接口业务规则中移除默认分页、鉴权注入占位、gRPC/网关映射、底层 Redis Key、回写细节等非核心业务描述。
- 验证结果：已查看 `服务设计.md` diff，确认每个接口仍保留业务规则，且内容更聚焦业务语义。

## 2026-05-29 21:43:57 +08:00

【新增说明：2026-05-29，本段记录消息幂等方案调整结果】

- 本次目标：按用户确认，移除消息日志表方案，改为使用业务表自身保证消息幂等。
- 已完成：删除 `auction_event_consume_log` 模型、自动迁移和事件日志仓储；简化 RocketMQ consumer，不再先写消息日志表；更新 `服务设计.md`，明确竞拍主表按 `version` 丢弃旧消息，出价记录按 `bid_record_id` 主键冲突丢弃重复消息。
- 设计变更：消息幂等不再依赖独立消息日志表；`bid_accepted` 事件处理时，出价记录和竞拍主表分别通过主键和版本号保证幂等。
- 验证结果：已运行 `gofmt`；按用户要求停止继续运行测试。本轮未完成测试验证。

## 2026-05-29 22:22:16 +08:00

【新增说明：2026-05-29，本段记录按更新后服务设计对齐结果】

- 本次目标：检查当前项目是否符合用户更新后的 `服务设计.md`，并将最后的设计补充同步到“竞拍数据异步同步”章节下。
- 已完成：将 RocketMQ consumer、业务幂等、重试和回写策略移动到 `## 竞拍数据异步同步` 下；补充 `auction_expire_check` Tag；修正 `bid_record_id` 注释为插入出价记录表。
- 已完成代码对齐：生产者使用 `auction_id` 作为 sharding key 并启用 hash queue selector，保证同一竞拍活动消息进入同一队列；consumer 启用顺序消费和单协程消费；`bid_record_id` 主键重复时直接 ack 丢弃消息，不再继续更新竞拍主表；竞拍主表仍按 `version` 丢弃旧消息。
- 验证结果：已运行 `gofmt`；按此前用户要求，本轮未运行测试。

## 2026-05-29 22:24:45 +08:00

【新增说明：2026-05-29，本段记录异步事件消息体文档更新结果】

- 本次目标：根据真实代码更新“竞拍数据异步同步”下各种事件消息的消息体。
- 已完成：在 `服务设计.md` 中补充统一事件外层结构、竞拍状态快照字段、`auction_started`、`bid_accepted`、`auction_finished`、`auction_failed`、`auction_cancelled`、`auction_expire_check` 的消息体示例。
- 设计变更：移除旧示例中的 `room_id`；明确时间字段口径，事件外层 `timestamp` 和 `bid_time` 使用 Unix 秒，快照中的 `start_time/end_time` 与延迟检查 `expire_at` 使用 Unix 毫秒。
- 验证结果：已使用 `rg` 检查事件类型和关键字段，确认文档中不再残留旧的 `room_id` 消息示例。

## 2026-05-31 16:52:00 +08:00

【新增说明：2026-05-31，本段记录 api-gateway 接入 auction-service 结果】

- 本次目标：在 `api-gateway` 中补充 `auction-service` HTTP 转发代码，先不加身份认证；生成可导入 Postman 的 HTTP 接口测试文件；完成后启动 `auction-service` 与 `api-gateway`。
- 已完成：新增 api-gateway auction gRPC client；新增 auction HTTP handler；在网关配置中增加 `auctionService.addr` 与 `API_GATEWAY_AUCTION_SERVICE_ADDR` 环境变量覆盖；注册 `/api/auction/auctions`、详情、按商品查询、更新、开始、结束、取消、删除、出价和出价记录列表等无认证 HTTP 路由；生成 `services/api-gateway/postman/auction-http.postman_collection.json`。
- 未完成：本轮按要求未接入身份认证，`shop_id` 暂由请求体或 query string 显式传入。
- 阻塞点：无。
- 设计变更：网关层将 auction 能力暴露为 HTTP JSON 接口，保留内部 `auction-service` gRPC-only 边界。
- 验证结果：已运行 `gofmt`；已运行 `go test ./services/api-gateway/...` 通过；已运行 `go test ./services/auction-service/...` 通过；已校验 Postman collection JSON 语法；已启动 `auction-service` 并确认 `127.0.0.1:9003` 监听；已启动 `api-gateway` 并确认 `127.0.0.1:58080` 监听；已请求 `/health` 返回 `code=0`；已请求 `/api/auction/auctions?shop_id=10001&page=1&page_size=1` 返回 `code=0`。
- 后续建议：后续接入身份认证后，将创建、更新、开始、结束、取消、删除等商户侧接口中的 `shop_id` 改为从可信身份注入，不再允许客户端直接传入。

## 2026-06-05 13:24:32 +08:00

【新增说明：2026-06-05，本段记录竞拍接口鉴权、路由和身份注入修复结果】

- 本次目标：修复 auction-service 和 api-gateway 中竞拍接口的身份来源、路由注册和 HTTP 转发结构，移除固定身份占位。
- 已完成：api-gateway 将竞拍 HTTP 基础路径从 `/api/auction/auctions` 调整为文档要求的 `/api/auctions`；公开暴露详情、按商品查询和出价记录列表；商家管理类接口创建、商铺列表、修改、开始、结束、取消、删除纳入商家 JWT 鉴权；用户出价纳入用户 JWT 鉴权。
- 已完成：api-gateway 竞拍创建、商铺列表、修改、开始、结束、取消、删除均从商家 context 注入 `shop_id`，不再读取请求体或 query 中的 `shop_id`；用户出价从用户 context 注入 `user_id`，不再信任请求体中的 `user_id`。
- 已完成：auction-service gRPC server 接入 `identity.UnaryServerInterceptor()`；商家类 RPC 统一从 `identity.ShopID(ctx)` 读取当前商铺身份；`PlaceBid` 从 `identity.UserID(ctx)` 读取当前用户身份；移除固定 `shop_id=10001` 占位。
- 已完成：竞拍 HTTP 返回结构按文档调整为直接 `data`：创建/详情返回竞拍对象，按商品和商铺列表返回文档字段，修改返回 `{}`，开始/结束/取消返回对应小对象，删除返回 `true`，出价返回出价结果对象；时间格式调整为 `YYYY-MM-DD HH:mm:ss`。
- 已完成：新增 api-gateway handler 测试，覆盖竞拍创建使用 context shop_id、用户出价使用 context user_id。
- 未完成：未调整 auction proto 字段，内部 gRPC 请求仍保留 `shop_id/user_id` 字段以兼容生成代码和内部调用结构，但受保护业务入口的身份来源以 metadata/context 为准。
- 阻塞点：无。
- 设计变更：竞拍外部 HTTP 路由按服务设计文档收敛到 `/api/auctions`；身份边界从请求参数显式传入调整为网关鉴权后通过 gRPC metadata 透传。
- 验证结果：已运行 `gofmt`；已运行 `$env:GOCACHE = Join-Path (Get-Location) '.tmp-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/... ./services/auction-service/...`，通过。
- 后续建议：更新 Postman collection 中旧的 `/api/auction/auctions` 路径，并补充用户端登录/签发 token 联调用例。

## 2026-06-05 13:34:36 +08:00

【新增说明：2026-06-05，本段记录竞拍服务错误提示中文化结果】

- 本次目标：将 auction-service 中对外返回的英文错误提示替换为更准确的中文提示。
- 已完成：将竞拍参数错误、身份缺失、竞拍不存在、重复创建、状态不允许、出价记录不存在、重复出价、出价过低、超过封顶价、竞拍过期、商品归属不匹配和内部错误提示改为中文。
- 已完成：将 Redis 结果格式异常、RocketMQ 事件格式异常和延迟检查缺少 `expire_at` 等内部错误提示改为中文；Redis Lua 脚本中的英文错误标记保留为内部匹配 key，不直接作为对外响应。
- 已完成：将 api-gateway 竞拍创建、修改、出价和时间解析入口的 `invalid request` 改为中文参数提示。
- 未完成：无。
- 阻塞点：无。
- 验证结果：已运行 `gofmt`；已运行 `$env:GOCACHE = Join-Path $env:TEMP 'livebid-go-build-cache'; go test ./services/api-gateway/... ./services/goods-service/... ./services/auction-service/...`，通过。

## 2026-06-05 13:39:04 +08:00

【新增说明：2026-06-05，本段记录用户出价调用边界修正结果】

- 本次目标：确认用户出价是否只通过 gRPC 供 `websocket-gateway` 远程调用，并通过 gRPC metadata 注入用户身份。
- 已完成：确认 `auction-service.PlaceBid` 从 `identity.UserID(ctx)` 读取 metadata/context 中的用户身份，不使用请求中的 `user_id` 作为可信身份。
- 已完成：移除 api-gateway 中用户出价 HTTP 路由 `POST /api/auctions/:id/bids`，移除对应 HTTP 转发 handler、请求/响应 DTO 和测试用例；api-gateway 仅保留查询出价记录 `GET /api/auctions/:id/bids`。
- 已完成：在 `服务设计.md` 末尾追加设计补充，明确 `PlaceBid` 不暴露 HTTP，由 `websocket-gateway` 校验用户登录态后通过 gRPC metadata 注入用户身份。
- 未完成：未调整 proto 中的 `user_id` 字段；该字段暂保留兼容生成代码和内部契约演进，服务端逻辑以 metadata 用户身份为准。
- 阻塞点：无。
