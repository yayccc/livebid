# 开发进度

## 2026-06-03 14:08:51 +08:00

- 本次目标：修改商铺服务与 api-gateway 中的商铺代理接口，使其按 `docs/services/shop-service/服务设计.md` 的注册、登录、按 ID 查询、按 ID 修改接口落地，并补充 `GET /api/shop/me` 查询个人信息接口。
- 已完成：`shop-service.GetShop`、`shop-service.UpdateShop` 改为按请求 `id` 查询和更新；api-gateway 新增 `GET /api/shop/:id`、`PUT /api/shop/:id`，并支持 `GET /api/shop/me`、`PUT /api/shop/me` 使用当前 JWT 商铺 ID；注册请求兼容文档中的 `shopName` 字段；响应中补充 `shopId` 和登录 `token` 字段。
- 已完成：补充服务层与网关 handler 聚焦测试，覆盖按路径 ID 查询、`/me` JWT 解析、更新 ID 校验与越权拦截。
- 设计变更：已在 `服务设计.md` 的接口表和正文中追加 `GET /api/shop/me` 查询个人信息接口说明。
- 未完成：尚未进行真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：已执行 `gofmt`；按用户要求跳过测试。此前沙箱内执行 `go test` 被 Windows Go build cache 权限拦截，未继续提权测试。
- 后续建议：联调时重点验证 Gin 路由中 `/api/shop/me` 与 `/api/shop/:id` 的匹配、JWT 签发与 gRPC metadata 透传。

## 2026-06-03 15:58:46 +08:00

- 本次目标：按用户要求再次收紧 `api-gateway` 和 `shop-service`，严格对齐 `服务设计.md` 的商铺接口格式。
- 已完成：注册接口请求字段固定为 `shopName`，响应 `data` 仅返回 `shopId`，消息为 `注册成功`；登录接口响应 `data` 仅返回 `token`，消息为 `登录成功`；查询商铺和查询个人信息响应 `data` 直接返回商铺对象，不再包裹 `shop` 字段，也不返回文档未列出的状态、审核和时间字段；修改接口只按 `/api/shop/{id}` 处理，不再把 `PUT /api/shop/me` 作为特殊入口。
- 已完成：`shop-service.GetShop` 和 `shop-service.UpdateShop` 保持按请求 `id` 执行，符合 `/api/shop/{id}` 代理语义。
- 设计变更：无新增设计，仅按既有设计收紧实现。
- 未完成：未做真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：按用户上轮要求不执行测试；本轮仅执行 `gofmt` 和静态 diff 检查。
- 后续建议：接口联调时以 `服务设计.md` 的请求体和返回值作为验收样例。

## 2026-06-03 16:46:27 +08:00

- 本次目标：修改商铺服务设计文档，统一所有商铺接口返回结构为 `code`、`message`、`data`，并补齐 `/api/shop/me` 接口实现。
- 已完成：`服务设计.md` 中查询商铺、修改商铺、查询个人信息返回示例均补齐 `message`，修改商铺接口新增返回值示例。
- 已完成：api-gateway 将 `GET /api/shop/me` 注册为显式路由，调用 `ShopHandler.GetCurrent`，不再依赖 `/:id` 的 `me` 特殊分支；查询和修改接口响应改为统一返回 `code/message/data`，通用响应体固定保留这三个字段。
- 设计变更：统一商铺 HTTP API 成功响应结构，消息文案使用 `注册成功`、`登录成功`、`查询成功`、`修改成功`。
- 未完成：未进行真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：已执行 `gofmt`；按用户前序要求未执行测试。
- 后续建议：联调时重点确认前端是否依赖旧的无 `message` 查询返回。

## 2026-06-03 16:55:03 +08:00

- 本次目标：检查并调整 `/api/shop/me` 和商铺更新接口的鉴权链路，确保经过 api-gateway JWT 解析后，通过 gRPC metadata 将身份传递给 shop-service。
- 已完成：api-gateway 新增按路径参数条件触发的商铺鉴权中间件，`/api/shop/me` 在命中 `/:id` 路由时会先执行商铺 JWT 解析；`PUT /api/shop/:id` 保持在商铺鉴权分组下。
- 已完成：api-gateway 的 `/me` handler 不再自行解析 Authorization，也不再把当前商铺 ID 写入 gRPC 请求体，而是依赖 gRPC client interceptor 透传身份 metadata。
- 已完成：shop-service 的 `GetShop` 在请求 `id <= 0` 时从 gRPC metadata/context 获取当前商铺 ID；`UpdateShop` 从 gRPC metadata/context 获取当前商铺 ID，并校验只能更新当前商铺。
- 设计变更：身份来源由网关请求体参数显式传入收敛为 gRPC metadata 透传，网关负责认证，shop-service 负责基于 metadata 做当前商铺识别和更新归属校验。
- 未完成：未进行真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：已执行 `gofmt`；按用户前序要求未执行测试。
- 后续建议：联调时重点验证 `/api/shop/me` 不带 token 返回 401，带商铺 token 后 shop-service 能从 metadata 获取身份。

## 2026-06-03 17:00:20 +08:00

- 本次目标：按用户要求只检查并修复路由保护位置，将获取当前商铺和更新商铺接口放到 `RequireShopAuth` 中间件后。
- 已完成：确认 `RequireShopAuth` 会解析商铺 JWT 并注入 request context，shop-service gRPC client 已配置 `identity.UnaryClientInterceptor()`，会将该身份写入 gRPC metadata。
- 已完成：将 `GET /api/shop/me` 和 `PUT /api/shop/:id` 挂载到 `merchant := api.Group("")` 且执行 `merchant.Use(middleware.RequireShopAuth(shopJWTManager))` 之后。
- 设计变更：无。
- 未完成：未进行真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：已执行 `gofmt`；按用户前序要求未执行测试。
- 后续建议：联调验证 `/api/shop/me` 与 `PUT /api/shop/:id` 无 token 时返回 401，带 token 时 shop-service 可从 metadata 获取商铺身份。

## 2026-06-03 17:05:52 +08:00

- 本次目标：修复 shop-service 相关接口实现，重点检查 `/api/shop/me` 和更新商铺接口，并将商铺相关接口错误 message 改为明确中文。
- 已完成：确认 `/api/shop/me` 与 `PUT /api/shop/:id` 已在 `RequireShopAuth` 中间件后，网关通过 gRPC client interceptor 将身份透传到 metadata，shop-service 从 metadata/context 获取当前商铺身份。
- 已完成：api-gateway 商铺注册、登录、更新请求参数错误、JWT 鉴权错误、gRPC 错误映射改为明确中文提示。
- 已完成：shop-service gRPC handler 与 repository 的商铺不存在、参数无效、身份缺失、无权修改、重复注册等错误改为中文业务提示。
- 设计变更：无。
- 未完成：未进行真实 MySQL 与多服务端到端验证。
- 阻塞点：无。
- 验证结果：已执行 `gofmt`；按用户前序要求未执行测试。
- 后续建议：联调时重点检查失败响应 message 是否满足前端展示需求。

## 2026-06-07 18:08:38 +08:00

【新增说明：2026-06-07，本段记录 mobile-user 聚合接口所需公开商铺批量查询能力补充结果】

- 本次目标：为 `mobile-user` 首页直播间流聚合接口补齐批量查询公开商铺信息能力，避免一页直播间产生 N+1 商铺查询。
- 已完成：扩展 `api/proto/shop/v1/shop.proto`，新增 `BatchGetPublicShops`；重新生成 `gen/proto/shop/v1` 代码；shop-service 仓储新增 `BatchFindByIDs`；gRPC handler 新增批量查询并按请求 ID 顺序返回。
- 业务口径：下游仍返回 `Shop` proto，用户侧字段边界由 `api-gateway` 严格过滤，只暴露 `shop_id`、`shop_name`、`logo`、`description` 等公开信息，不返回手机号、邮箱等商家管理字段。
- 已完成测试：补充 shop-service handler 测试，覆盖批量查询结果按请求顺序返回。
- 验证结果：已运行 `gofmt`；已运行 `go test ./services/auction-service/... ./services/shop-service/... ./services/api-gateway/...`，通过。
- 后续建议：后续如果其他用户侧接口也需要公开商铺资料，可复用该 RPC，但仍应在网关侧按页面场景过滤字段。
