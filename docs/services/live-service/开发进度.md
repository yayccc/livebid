# Live Service 开发进度

## 2026-06-06 18:22:29 +0800

- 本次目标：排查 OBS 推流成功、SRS 回调成功且直播间 online，但 WebRTC 播放页连接失败的问题。
- 已完成：
  - 确认 SRS HTTP API `1985` 可访问，`/api/v1/streams/` 显示目标流 `publish.active=true`，OBS 到 SRS 推流链路正常。
  - 确认 `live-service` 已收到 `on_publish` 回调并将 `media_stream_status` 置为在线，业务状态链路正常。
  - 定位播放失败集中在浏览器到 SRS 的 WebRTC ICE/DTLS 媒体链路：播放信令成功返回 session，但 SRS 日志出现 `DTLS_HANG` 和 session timeout。
  - 更新 Compose 的 SRS 启动命令，支持通过 `SRS_PUBLIC_HOST` 或 `SRS_RTC_CANDIDATE` 覆盖 WebRTC SDP candidate，并让 `live-service` 的推/拉流地址使用同一个公开主机。
  - 更新 `tools/srs-webrtc-player`：代理转发时将请求体中的 `api` 改写为真实 SRS API，避免 SRS 日志显示为测试页代理端口；页面增加 ICE 状态、gathering 状态、candidate pair 和 loopback candidate 提示。
  - 更新 `docs/services/live/srs.md`，补充本地 Docker/WSL WebRTC 播放排查说明。
- 未完成：未在浏览器中完成最终播放验证；需要按当前浏览器所在网络设置正确的 `SRS_PUBLIC_HOST` 或 `SRS_RTC_CANDIDATE` 后重启 SRS/live-service 再验证。
- 阻塞点：WebRTC UDP candidate 必须是浏览器可访问地址；若浏览器与 SRS 不在同一网络命名空间，`127.0.0.1` 可能无法完成 ICE/DTLS。
- 验证结果：
  - `curl http://127.0.0.1:1985/api/v1/versions`：通过。
  - `curl http://127.0.0.1:1985/api/v1/streams/`：通过，目标流在线。
  - `docker compose -f deployments/docker-compose.yml logs srs/live-service/api-gateway`：确认 SRS play session 创建、随后 DTLS timeout；确认 live-service 回调成功。

## 2026-06-08 16:16:04 +0800

- 本次目标：阅读 live-service 文档和代码，补齐商铺管理后台直播间管理界面，并在竞拍管理中补充直播间信息和创建竞拍时的 RoomID 选择。
- 已完成：
  - 在 `docs/services/live-service/api.md` 追加商家视角 `GET /api/merchant/live/rooms` 设计说明。
  - 扩展 `api/proto/live/v1/live.proto` 的 `ListLiveRoomsRequest`，新增可选 `shop_id/status` 筛选，并重新生成 `gen/proto/live/v1`。
  - 更新 live-service 列表逻辑：未传筛选时保持用户侧直播广场只查 `living`；传入 `shop_id` 时支持商家查询本店直播间。
  - 在 api-gateway 新增 `ListMerchantLiveRooms`，并注册 `GET /api/merchant/live/rooms`。
  - 商家后台新增直播管理页面，可列表筛选、创建直播间、开播/关播、查看推流信息和创建时返回的一次性推流码。
  - 竞拍管理页新增直播间下拉选项，创建竞拍请求会传递 `room_id`，列表和详情中补充直播间信息。
- 未完成：
  - 本轮按用户要求未继续执行 Go 测试。
  - 直播间基础信息修改接口仍未实现，当前页面只支持创建和状态操作。
- 阻塞点：
  - 无核心阻塞。Go 测试首次执行时曾因默认 Go build cache 权限失败，后改用工作区缓存后编译耗时较长，用户明确表示不需要测试。
- 设计变更：
  - 商家后台直播间列表使用独立路径 `GET /api/merchant/live/rooms`，避免复用公开 `GET /api/live/rooms` 造成用户直播广场和商家管理语义混淆。
  - 推流敏感字段仍通过 `GET /api/live/rooms/{id}/stream` 获取，列表接口不返回 `stream_name/rtmp_push_url/webrtc_play_url`。
- 验证结果：
  - 已执行 `npm run build`，通过。
  - 已执行 `npm run lint`，通过。
  - 已执行 `gofmt` 格式化相关 Go 文件。
  - Go 聚焦测试未完成，原因见“阻塞点”。
- 后续建议：
  - 后续如需支持编辑直播间标题、封面和简介，应补充 `UpdateLiveRoom` 的 proto、live-service handler/repository 更新字段和 api-gateway 路由。
  - 若开播前必须确认 OBS 已实际推流，可将 `media_stream_status=online` 纳入开始竞拍前校验。
