# Live Service 开发进度

## 2026-06-06 18:22:29 +0800

- 本次目标：排查 OBS 推流成功、SRS 回调成功且直播间 online，但 WebRTC 播放页连接失败的问题。
- 已完成：
  - 确认 SRS HTTP API `1985` 可访问，`/api/v1/streams/` 显示目标流 `publish.active=true`，OBS 到 SRS 推流链路正常。
  - 确认 `live-service` 已收到 `on_publish` 回调并将 `media_stream_status` 置为在线，业务状态链路正常。
  - 定位播放失败集中在浏览器到 SRS 的 WebRTC ICE/DTLS 媒体链路：播放信令成功返回 session，但 SRS 日志出现 `DTLS_HANG` 和 session timeout。
  - 更新 Compose 的 SRS 启动命令，支持通过 `SRS_PUBLIC_HOST` 或 `SRS_RTC_CANDIDATE` 覆盖 WebRTC SDP candidate，并让 `live-service` 的推/拉流地址使用同一个公开主机。
  - 更新 `tools/srs-webrtc-player`：代理转发时将请求体中的 `api` 改写为真实 SRS API，避免 SRS 日志显示为测试页代理端口；页面增加 ICE 状态、gathering 状态、candidate pair 和 loopback candidate 提示。
  - 更新 `docs/services/live/srs.md`，补充本地 Docker/WSL WebRTC 播放排查说明。
- 未完成：未在浏览器中完成最终播放验证；需要按当前浏览器所在网络设置正确的 `SRS_PUBLIC_HOST` 或 `SRS_RTC_CANDIDATE` 后重启 SRS/live-service 再验证。
- 阻塞点：WebRTC UDP candidate 必须是浏览器可访问地址；若浏览器与 SRS 不在同一网络命名空间，`127.0.0.1` 可能无法完成 ICE/DTLS。
- 验证结果：
  - `curl http://127.0.0.1:1985/api/v1/versions`：通过。
  - `curl http://127.0.0.1:1985/api/v1/streams/`：通过，目标流在线。
  - `docker compose -f deployments/docker-compose.yml logs srs/live-service/api-gateway`：确认 SRS play session 创建、随后 DTLS timeout；确认 live-service 回调成功。
