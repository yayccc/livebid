# 开发进度

## 2026-06-05 17:04:03 +08:00

【新增说明：2026-06-05，本段记录用户端 JWT issuer 对齐】

### 本次目标

- 将 user-service 签发的用户端 JWT issuer 与 api-gateway 用户鉴权和 ws-gateway WebSocket 鉴权统一为 `livebid-user`。

### 已完成

- 将 `services/user-service/internal/config/config.go` 默认 JWT issuer 从 `livebid` 调整为 `livebid-user`。
- 将 `services/user-service/configs/config.local.yaml` 本地 JWT issuer 调整为 `livebid-user`。
- 更新 `services/user-service/README.md` 中的 `USER_SERVICE_JWT_ISSUER` 示例。

### 验证结果

- 已执行 `gofmt -w services/user-service/internal/config/config.go`。
- 已执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/user-service/...`，通过。

## 2026-05-31 13:16:50 +08:00

【新增说明：2026-05-31，本段记录本次 user-service 开发结果】

### 本次目标

- 阅读 `docs/services/user-service/` 下用户服务文档和鉴权文档，先完成需求澄清。
- 按确认后的方案开发 gRPC-only `user-service`，优先编写 proto 并生成 `gen/proto/user/v1` 代码。
- 仿照 `shop-service` 完成 model、repository、handler、router、bootstrap、config、README、Makefile。

### 已完成

- 已阅读用户服务需求/设计文档、`docs/04-auth-design.md`、`shop-service` 和 `goods-service` 的核心实现。
- 已在 `docs/services/user-service/服务设计.md` 追加“待确认问题”和“设计补充”，保留原文不删除。
- 已新增 `api/proto/user/v1/user.proto`，并生成 `gen/proto/user/v1/user.pb.go`、`gen/proto/user/v1/user_grpc.pb.go`。
- 已从 `templates/go-service` 创建 `services/user-service`，并补齐：
  - `cmd/server/main.go`
  - `configs/config.local.yaml`
  - `internal/config/config.go`
  - `internal/bootstrap/app.go`
  - `internal/router/grpc.go`
  - `internal/model/user.go`
  - `internal/model/address.go`
  - `internal/repository/mysql.go`
  - `internal/repository/user_repository.go`
  - `internal/repository/address_repository.go`
  - `internal/handler/user_grpc_handler.go`
  - `README.md`
  - `Makefile`
- 已实现用户注册、登录签发 JWT、查询公开用户资料、修改当前用户资料、地址新增/列表/详情/修改/删除/设默认。
- 登录使用 `pkg/auth` 签发 HS256 JWT，默认有效期 7 天。
- 当前用户资料和地址接口从 gRPC metadata `livebid-auth-subject` 读取当前 user_id。
- 地址默认设置使用事务保证同一用户仅一个默认地址。

### 未完成

- 本阶段不实现头像 multipart 上传接口，仅保存头像 URL。
- 本阶段不新增 `user_login_log` 表，也不记录用户登录日志。
- 尚未接入 `api-gateway` 的 HTTP 路由、JWT 校验和 gRPC client。
- 未补充数据库级别的 `phone`、`email` 非空唯一约束；当前按确认结果由仓储层对非空值做唯一校验，数据库使用普通索引。

### 阻塞点

- 无编码阻塞。
- 头像上传依赖对象存储或文件服务，当前设计确认不在 `user-service` 实现。
- 网关接入需要后续在 `services/api-gateway` 中补充 HTTP 到 gRPC 的适配。

### 设计变更

- 登录由 `user-service` 直接签发 JWT，而不是返回 opaque token。
- `GetUser(id)` 允许内部服务按任意用户 ID 查询公开资料。
- 当前用户资料和地址接口统一以 metadata `livebid-auth-subject` 作为身份来源。
- 修改地址采用部分更新语义。
- 用户主表名保持为 `user`。
- `phone`、`email` 非必填时允许为空，唯一性只对非空值做仓储层校验。

### 验证结果

- 已执行 `protoc --go_out=. --go_opt=module=github.com/yayccc/livebid --go-grpc_out=. --go-grpc_opt=module=github.com/yayccc/livebid api/proto/user/v1/user.proto`，生成成功。首次执行因本机缺少 `protoc-gen-go` 失败，随后安装 `protoc-gen-go` 与 `protoc-gen-go-grpc` 后成功。
- 已执行 `gofmt` 格式化新增 Go 文件。
- 已执行 `go test ./services/user-service/...`，通过。
- 已执行 `go test ./...`，失败点为既有 `pkg/logger` 测试 `TestNewWritesJSONToFile` 在 Windows 下清理临时日志文件时提示文件仍被占用；`user-service` 相关包均编译通过。

### 后续建议

- 在 `api-gateway` 中补充用户 HTTP API、JWT 鉴权和 gRPC client。
- 后续如需要审计或风控，再补充 `user_login_log` 表与登录日志仓储。
- 若生产环境需要强一致唯一约束，建议将 `phone`、`email` 改为可空字段并设计数据库层唯一索引策略。

## 2026-06-03 22:55:50 +08:00

【新增说明：2026-06-03，本段记录本次 user-service 接入 api-gateway 的结果】

### 本次目标

- 将用户服务接口接入 `api-gateway`，参考现有商铺、商品、直播服务的接入方式。

### 已完成

- 新增 `api-gateway` 到 `user-service` 的 gRPC client 构造。
- 在 `api-gateway` 配置中新增 `userService.addr`、`userService.target` 及 `API_GATEWAY_USER_SERVICE_ADDR`、`API_GATEWAY_USER_SERVICE_TARGET` 环境变量覆盖。
- 在 `api-gateway` bootstrap 中初始化并关闭 user-service gRPC 连接，创建 `UserHandler`。
- 新增用户 HTTP handler，覆盖注册、登录、查询用户信息、修改当前用户资料、地址新增/列表/详情/修改/删除/设默认。
- 在 HTTP 路由中接入 `/api/users/*`，用户资料修改和地址接口使用用户端 JWT 鉴权。
- 用户头像上传接入复用 `/api/files/upload` 的对象存储能力，新增 `/api/users/avatar/upload` 路径。
- 修复 `user-service` 受保护接口身份读取方式：接入公共 `identity.UnaryServerInterceptor()`，从统一 identity context 读取当前 user_id，匹配 `api-gateway` gRPC client 的 metadata 透传约定。
- 将 `user-service` 本地默认 gRPC 端口调整为 `:9004`，避免与现有 `auction-service` 本地默认 `:9003` 冲突；`api-gateway` 本地配置同步指向 `127.0.0.1:9004`。
- 更新 `services/api-gateway/README.md` 和 `services/user-service/README.md`。
- 新增/更新单测覆盖用户 handler、配置 target 覆盖和 user-service 当前用户身份读取。

### 未完成

- 未进行真实端到端联调；本轮只执行了自动化测试。

### 阻塞点

- 无编码阻塞。

### 设计变更

- 将旧的 `livebid-auth-subject` 单字段 metadata 读取方式调整为公共 identity metadata/context 方案，即 `livebid-auth-subject-type` 与 `livebid-auth-subject-id`，与 `docs/04-auth-design.md` 和已有网关 client 拦截器保持一致。
- 用户登录 token 继续由 `user-service` 签发，`api-gateway` 只透传 `access_token` 并统一 HTTP 响应字段为 `token`。

### 验证结果

- 已执行 `gofmt` 格式化本次新增/修改的 Go 文件。
- 已执行 `go test ./services/api-gateway/... ./services/user-service/...`，首次因默认 Go build cache 目录 `/home/yaya/.cache/go-build` 在当前沙箱只读而失败。
- 已改用临时缓存执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/api-gateway/... ./services/user-service/...`，通过。

### 后续建议

- 后续端到端验证时同时启动 `user-service` 和 `api-gateway`，用用户登录得到的 token 访问地址接口，确认 metadata 透传和权限隔离。

## 2026-06-03 23:13:35 +08:00

【新增说明：2026-06-03，本段记录用户头像上传路由鉴权调整】

### 本次目标

- 确认 `/api/users/avatar/upload` 是否需要认证，并修正网关路由。

### 已完成

- 将 `/api/users/avatar/upload` 从公开 `/api/users` 分组移动到用户 JWT 鉴权分组。
- 更新 `services/api-gateway/README.md`，明确头像上传需要用户 JWT。
- 新增路由测试，覆盖未携带 token 访问用户头像上传接口返回 `401`。

### 验证结果

- 已执行 `gofmt` 格式化路由与测试文件。
- 已执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/api-gateway/... ./services/user-service/...`，通过。
