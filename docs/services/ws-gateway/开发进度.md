# 开发进度

## 2026-06-09 14:30:52 +08:00

【新增说明：2026-06-09，本段记录 WebSocket 出价主链路和响应分发字段文档同步】

- 本次目标：将用户端出价链路调整为 WebSocket `place_bid` 优先、HTTP 出价作为降级入口，并同步 WebSocket 响应协议文档。
- 已完成：在 `服务设计.md` 的服务端响应结构中补充 `request_type` 字段，明确 `type=response` 只是通用响应信封，前端应按 `request_type` 精确分发响应。
- 已完成：补充 `request_type` 取值口径：普通请求与客户端消息类型一致，包括 `ping`、`place_bid`、`room_leave`；连接建立使用 `connect`；无法解析或空类型使用 `malformed`。
- 已完成：在 `place_bid` 业务规则中明确 WebSocket 是推荐出价主链路，HTTP `POST /api/auctions/{id}/bids` 仅作为 WebSocket 不可用、重连中或旧客户端兼容时的降级入口。
- 已完成：更新 `services/ws-gateway/README.md`，补充 `request_type` 响应示例和出价入口推荐口径。
- 未完成：未修改前端代码；前端仍需按 `request_type` 分发通用 `response`，并实现 WebSocket 不可用时的 HTTP fallback。
- 验证结果：文档更新，无需运行服务测试；代码层上一轮已通过 `go test ./services/ws-gateway/...`。

## 2026-06-06 15:53:08 +08:00

【新增说明：2026-06-06，本段记录在线人数展示聚合与心跳间隔调整】

- 本次目标：将 ws-gateway 心跳调整为 30 秒，并将直播间在线人数展示广播改为服务端聚合后定时推送。
- 已完成：将默认心跳建议间隔调整为 30 秒、心跳超时时间调整为 90 秒；新增 `WS_GATEWAY_ONLINE_BROADCAST_INTERVAL_SECONDS`，默认 3 秒。
- 已完成：在线状态仍在进入、心跳、离开和超时清理时实时写入 Redis；面向客户端的 `room_online_changed` 改由 ws-gateway 按房间聚合，默认每 3 秒读取 Redis 最新人数后最多广播一次。
- 设计结论：在线人数展示聚合 ticker 放在 ws-gateway 内部，不放在 api-gateway；api-gateway 不持有 WebSocket 连接和房间订阅关系，不参与实时 presence 推送。
- 未完成：尚未做真实多实例 Redis Pub/Sub 联调，暂以编译、单元测试和配置校验为主。
- 验证结果：待执行。

## 2026-06-05 17:23:42 +08:00

【新增说明：2026-06-05，本段记录 ws-gateway 首版代码实现结果】

- 本次目标：按 `docs/services/ws-gateway` 设计实现 WebSocket 网关首版服务。
- 已完成：新增 `services/ws-gateway` 服务目录，包含 HTTP/WebSocket 服务入口、配置加载、bootstrap、router、auction/live gRPC client、WebSocket handler、本地连接 Hub、RocketMQ 广播消费器、Redis 在线人数 repository、README 和 Makefile。
- 已完成：实现 `GET /ws/live?room_id={room_id}&token={access_token}`；URL token 按用户端 JWT 校验，只接受 `livebid-user` issuer；token 为空时按游客连接处理，游客只能观看和接收广播。
- 已完成：实现 `ping`、`place_bid`、`room_leave` 客户端消息；出价时使用连接绑定的 `user_id`，并通过 gRPC metadata 向 auction-service 透传用户身份，同时传入当前连接 `room_id`。
- 已完成：实现 Redis 在线状态 Lua 脚本，进入、心跳、离开和房间清理都基于有效连接重算用户最大 `expire_at`，在线人数按去重 `identity_key` 统计，连接数单独统计。
- 已完成：实现 Redis Pub/Sub 在线人数多实例广播；实现 RocketMQ `auction_event` 广播消费模式，并按实例内短期 `event_id` 去重，不做 Redis 全局去重；`auction_expire_check` 不对前端广播。
- 已完成：WebSocket 对外 JSON 时间字段统一为 Unix 毫秒；消费 auction-service 事件时将秒级 `timestamp`、`bid_time` 转为毫秒。
- 未完成：尚未做真实 Redis/RocketMQ/live-service/auction-service 联调；未实现客户端断线后竞拍状态补偿查询；未接入部署编排和监控告警。
- 设计变更：本地 `config.local.yaml` 默认关闭 RocketMQ consumer，部署时通过 `WS_GATEWAY_ROCKETMQ_ENABLED=true` 和 `WS_GATEWAY_ROCKETMQ_NAME_SERVER` 开启，避免本地无 RocketMQ 时无法启动。
- 验证结果：已执行 `go get github.com/gorilla/websocket@v1.5.3`；已执行 `gofmt`；已执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/ws-gateway/...`，通过；已执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/user-service/...`，通过；已短暂启动 `go run ./services/ws-gateway/cmd/server` 并请求 `GET /health`，返回 `{"code":0,"message":"ok"}`。
- 后续建议：启动本地 Redis、live-service、auction-service 后做 WebSocket 手工联调；再启动 RocketMQ 验证多实例广播消费和实例重启后的客户端状态修正策略。

## 2026-06-05 17:04:03 +08:00

【新增说明：2026-06-05，本段记录 WebSocket 鉴权边界与用户 JWT issuer 对齐】

- 本次目标：确认 WebSocket 鉴权按 ws-gateway 设计执行，客户端通过 URL query 传入 token，并将 user-service 默认用户端 JWT issuer 对齐为 `livebid-user`。
- 已完成：更新 `docs/04-auth-design.md`，明确 HTTP 请求 JWT 由 `api-gateway` 验证，WebSocket 连接 JWT 由 `ws-gateway` 验证；WebSocket 当前使用 `GET /ws/live?room_id={room_id}&token={access_token}` 传入用户端 token。
- 已完成：更新 `docs/services/ws-gateway/服务设计.md` 待对齐事项，标记 WebSocket 鉴权边界、URL query token 方案和 user-service issuer 对齐已确认。
- 已完成：将 `services/user-service` 默认 JWT issuer、`configs/config.local.yaml` 和 README 示例从 `livebid` 调整为 `livebid-user`，与 api-gateway 用户鉴权和 ws-gateway WebSocket 鉴权保持一致。
- 未完成：尚未实现 ws-gateway 代码和实际 WebSocket 鉴权逻辑。
- 验证结果：已执行 `gofmt -w services/user-service/internal/config/config.go`；已执行 `git diff --check -- docs/04-auth-design.md docs/services/ws-gateway/服务设计.md docs/services/ws-gateway/开发进度.md services/user-service/internal/config/config.go services/user-service/configs/config.local.yaml services/user-service/README.md`，通过；已执行 `GOCACHE=/tmp/livebid-go-build-cache go test ./services/user-service/...`，通过。

## 2026-06-05 16:58:26 +08:00

【新增说明：2026-06-05，本段记录 ws-gateway 服务设计 review 后的文档调整】

- 本次目标：根据设计 review 结果，先修正时间单位和 Redis 在线人数去重原子性问题，其他风险简短记录为后续待考虑事项。
- 已完成：将 WebSocket 对外 JSON 消息时间字段统一为 Unix 毫秒，覆盖 `timestamp`、`server_time`、`expire_at`、`start_time`、`end_time` 等字段，并明确 ws-gateway 消费 auction-service 秒级事件字段时需要转换为毫秒。
- 已完成：补充 Redis 在线人数“用户有效连接重算规则”，要求进入、心跳、离开和超时清理都通过 Lua 基于仍有效连接重新计算 `identity_key` 的最大 `expire_at`，避免同一用户多连接时旧连接覆盖新连接过期时间或残留连接导致在线人数偏高。
- 已记录：WebSocket 鉴权信任边界、URL query 携带 access token、RocketMQ 广播消费模式漏消费恢复口径，暂放入 `服务设计.md` 的待对齐事项，后续再统一决策。
- 未完成：尚未修改 `docs/04-auth-design.md`，尚未编写 ws-gateway 代码、配置、README 或测试。
- 验证结果：已执行 `git diff --check -- docs/services/ws-gateway/服务设计.md docs/services/ws-gateway/开发进度.md`，通过。

## 2026-06-04 16:49:21 +08:00

【新增说明：2026-06-04，本段记录 ws-gateway 依赖的竞拍 `room_id` 对齐结果】

- 本次目标：补齐 ws-gateway 后续开发所依赖的竞拍 `room_id` 字段和归属校验口径。
- 已完成：auction-service 的 proto、模型、Redis 竞拍状态、事件生产消费和 api-gateway 转发已补齐 `room_id`；出价时由调用方传入当前连接所属 `room_id`，auction-service 校验 `auction_id` 与 `room_id` 一致后才允许出价。
- 设计结论：ws-gateway 不需要本地判断竞拍归属，只需要在 `place_bid` 转发时使用当前 WebSocket 连接绑定的 `room_id` 调用 auction-service。
- 待对齐：WebSocket JWT 鉴权规则仍需要同步更新 `docs/04-auth-design.md`。
- 验证结果：本轮代码侧验证记录见 `docs/services/auction-service/开发进度.md`。

## 2026-06-04 16:20:44 +08:00

【新增说明：2026-06-04，本段记录 ws-gateway 需求与服务设计结果】

- 本次目标：根据竞拍服务设计，完成 `ws-gateway` 的核心需求分析和服务设计文档。
- 已完成：新增 `需求分析.md`，明确连接管理、直播间订阅、出价转发、竞拍事件广播、在线人数统计和本期边界；新增 `服务设计.md`，覆盖 WebSocket 入口、统一消息格式、客户端消息、服务端广播事件、Redis 在线状态、多实例广播、RocketMQ 竞拍事件消费、错误码、配置项和本期不实现范围。
- 设计结论：第一版单个 WebSocket 连接只绑定一个直播间；观看可允许游客连接，出价必须用户登录；在线人数按直播间去重用户数展示，同时保留连接数；竞拍事件消费采用 RocketMQ 广播消费模式，避免多实例下只有一个实例能向本地连接广播。
- 未完成：尚未编写 ws-gateway 代码、proto、配置、README 或测试。
- 待对齐：auction-service proto 和代码中的 `room_id` 已于 2026-06-04 对齐；WebSocket JWT 鉴权规则需要同步更新 `docs/04-auth-design.md`。
- 验证结果：已运行 `git diff --check -- docs/services/ws-gateway/服务设计.md`，通过。
